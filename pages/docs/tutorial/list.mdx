---
keywords: "list crdt, movable list, 元素移动, 光标, awareness"
description: "如何使用 Loro 的 List 与 MovableList CRDT，并展示相关 API。"
---

# List 与 MovableList

Loro 提供两种列表：`List` 与 `MovableList`。`List` 支持插入、删除；`MovableList` 额外支持 `set` 与 `move`。

使用插入/删除可在 `List` 中模拟 set/move，但在并发场景会失效：同一元素被同时 set 或 move 时，模拟方案会删除原元素并插入两个新元素，结果不符合预期。

`MovableList` 可以正确处理这些情况，不过需要额外开销：仅执行插入/删除时，它在编解码上大约慢 80%，内存开销约高 50%。

两者都基于 [_Fugue_](https://arxiv.org/abs/2305.00583) 实现最大非交错性；`MovableList` 的移动能力额外使用 [_Moving Elements in List CRDTs_](https://martin.kleppmann.com/2020/04/27/papoc-list-move.html) 算法。

## 基本用法

### List

```ts twoslash
import { LoroDoc, Loro } from "loro-crdt";
import { expect } from "expect";
// ---cut---
const docA = new LoroDoc();
docA.setPeerId("1");
const listA = docA.getList("list");
listA.push(0);
listA.push(1);
listA.push(2);
const bytes: Uint8Array = docA.export({ mode: "snapshot" });

const docB = Loro.fromSnapshot(bytes);
docB.setPeerId("2");
const listB = docB.getList("list");
{
  // docA、docB 并发修改索引 2
  listB.delete(2, 1);
  listB.insert(2, 9);
  expect(docB.toJSON()).toStrictEqual({ list: [0, 1, 9] });
  listA.delete(2, 1);
  listA.insert(2, 8);
  expect(docA.toJSON()).toStrictEqual({ list: [0, 1, 8] });
}

{
  docA.import(docB.export({ mode: "update", from: docA.version() }));
  docB.import(docA.export({ mode: "update", from: docB.version() }));
}

expect(docA.toJSON()).toStrictEqual({ list: [0, 1, 8, 9] });
expect(docB.toJSON()).toStrictEqual({ list: [0, 1, 8, 9] });
```

### MovableList

```ts twoslash
import { LoroDoc, Loro } from "loro-crdt";
import { expect } from "expect";
// ---cut---
const docA = new LoroDoc();
docA.setPeerId("1");
const listA = docA.getMovableList("list");
listA.push(0);
listA.push(1);
listA.push(2);
const bytes: Uint8Array = docA.export({ mode: "snapshot" });

const docB = Loro.fromSnapshot(bytes);
docB.setPeerId("2");
const listB = docB.getMovableList("list");
{
  listA.set(2, 8);
  expect(docA.toJSON()).toStrictEqual({ list: [0, 1, 8] });
  listB.set(2, 9);
  expect(docB.toJSON()).toStrictEqual({ list: [0, 1, 9] });
}

{
  docA.import(docB.export({ mode: "update", from: docA.version() }));
  docB.import(docA.export({ mode: "update", from: docB.version() }));
}

// 因 docB 的 peerId 更大，最终收敛为 [0, 1, 9]
expect(docA.toJSON()).toStrictEqual({ list: [0, 1, 9] });
expect(docB.toJSON()).toStrictEqual({ list: [0, 1, 9] });

{
  listA.move(0, 2);
  listB.move(0, 1);
  expect(docA.toJSON()).toStrictEqual({ list: [1, 9, 0] });
  expect(docB.toJSON()).toStrictEqual({ list: [1, 0, 9] });
}

{
  docA.import(docB.export({ mode: "update", from: docA.version() }));
  docB.import(docA.export({ mode: "update", from: docB.version() }));
}

expect(docA.toJSON()).toStrictEqual({ list: [1, 0, 9] });
expect(docB.toJSON()).toStrictEqual({ list: [1, 0, 9] });
```

## List 上的光标

List 的光标会随列表变动：前方插入 → 光标右移；前方删除 → 左移；后方改动则不受影响。可用于稳定表示段落等结构的选区。

```ts twoslash
import {
  Cursor,
  LoroDoc,
  LoroList,
  LoroMovableList,
  LoroText,
} from "loro-crdt";
// ---cut---
const doc = new LoroDoc();
doc.setPeerId("1");
const list = doc.getList("list");
list.push("Hello");
list.push("World");
const cursor = list.getCursor(1)!;
console.log(cursor.pos()); // { peer: "1", counter: 1 }

const encodedCursor: Uint8Array = cursor.encode();
const exported: Uint8Array = doc.export({ mode: "snapshot" });

const docB = new LoroDoc();
docB.setPeerId("2");
const listB = docB.getList("list");
docB.import(exported);
listB.insert(0, "Foo");
console.log(docB.toJSON()); // { list: ["Foo", "Hello", "World"] }
const cursorB = Cursor.decode(encodedCursor);
{
  const pos = docB.getCursorPos(cursorB);
  console.log(pos.offset); // 2
}
listB.insert(1, "Bar");
console.log(docB.toJSON()); // { list: ["Foo", "Bar", "Hello", "World"] }
{
  const pos = docB.getCursorPos(cursorB);
  console.log(pos.offset); // 3
}
listB.delete(3, 1);
console.log(docB.toJSON()); // { list: ["Foo", "Bar", "Hello"] }
{
  const pos = docB.getCursorPos(cursorB);
  console.log(pos.offset); // 3
  console.log(pos.update); // { peer: "2", counter: 1 }
  const newCursor: Cursor = pos.update!;
  console.log(newCursor.pos()); // undefined（位于列表末尾）
  console.log(newCursor.side()); // 1（位于右端）
  const newPos = docB.getCursorPos(newCursor);
  console.log(newPos.offset); // 3
  console.log(newPos.update); // undefined
}
```
