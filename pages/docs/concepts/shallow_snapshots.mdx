# 浅快照（Shallow Snapshots）

## 快速概览

**浅快照** 类似于 Git 的浅克隆：保留当前状态，同时裁剪旧的历史。它是满足隐私合规、优化存储与提升性能的关键工具。

## 基本用法

```ts twoslash
import { LoroDoc } from "loro-crdt";
// ---cut---
const doc = new LoroDoc();
// ... 这里经历了大量编辑历史 ...

// 常规快照 —— 包含完整历史
const full = doc.export({ mode: "snapshot" });

// 浅快照 —— 裁剪后的历史
const shallow = doc.export({
  mode: "shallow-snapshot",
  frontiers: doc.frontiers(),
});

// 通常可以缩小 70%~90%
console.log(`Size reduction: ${100 - (shallow.length / full.length * 100)}%`);
```

## 内容删除（Redaction）

```ts twoslash
import { LoroDoc } from "loro-crdt";
// ---cut---
const doc = new LoroDoc();
const text = doc.getText("content");

// 写入敏感数据
text.insert(0, "SSN: 123-45-6789. ");
text.insert(18, "Public info.");

// 删除敏感部分
text.delete(0, 18);

// 生成干净的快照
const redacted = doc.export({
  mode: "shallow-snapshot",
  frontiers: doc.frontiers(),
});
// 历史中的敏感数据被永久移除
```

## 同步限制

⚠️ **注意**：只有在浅快照时间点之后拥有版本的节点才能继续同步。

## 常见模式

### 归档并裁剪
```ts twoslash
import { LoroDoc } from "loro-crdt";
// ---cut---
async function archiveAndTrim(doc: LoroDoc) {
  // 1. 归档完整历史
  const full = doc.export({ mode: "snapshot" });
  await saveToArchive(full);
  
  // 2. 为活跃使用生成浅快照
  const shallow = doc.export({
    mode: "shallow-snapshot",
    frontiers: doc.frontiers(),
  });
  
  return shallow;
}

async function saveToArchive(data: Uint8Array) {
  // 保存到冷存储
}
```

### 隐私优先的设计
```ts twoslash
import { LoroDoc } from "loro-crdt";
// ---cut---
class PrivacyDoc {
  constructor(private doc: LoroDoc) {}
  
  async redactSensitive() {
    // 删除敏感内容
    this.doc.getText("private").delete(0, this.doc.getText("private").length);
    
    // 生成干净的快照
    return this.doc.export({
      mode: "shallow-snapshot",
      frontiers: this.doc.frontiers(),
    });
  }
}
```

## 最佳实践

- **裁剪前先协调**：确保所有节点已完成同步
- **删除前先归档**：如需保留完整历史，先做备份

## 相关文档

- [编码](../tutorial/encoding)——了解不同导出选项
- [版本教程](../tutorial/version)——管理文档版本
- [Event Graph Walker](./event_graph_walker)——支撑浅快照的算法
